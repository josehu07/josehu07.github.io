[user]
    email = me@josehu.com
    name = Guanzhou Hu
[init]
    defaultbranch = main
[pull]
    rebase = false
[core]
    editor = vim
    pager = delta
[interactive]
    diffFilter = delta --color-only
[delta]
    navigate = true
    side-by-side = true
[merge]
    conflictStyle = zdiff3
[alias]
    alias = "!git config --get-regexp ^alias\\. | awk '/^alias\\./ { gsub(/^alias\\./, \"\"); printf \"%6s = %s\\n\", $1, substr($0, length($1)+2) }'"
    s = status -s
    st = status
    cfg = config --global --editor
    a = add
    aa = add -A
    b = branch
    ba = branch -a
    bm = branch -M
    bdel = branch -D
    c = commit
    cm = commit -m
    cam = commit -am
    camd = commit -a --amend
    m = merge
    mc = merge --continue
    ma = merge --abort
    rb = rebase
    rbi = rebase --interactive
    rbc = rebase --continue
    rba = rebase --abort
    co = checkout
    cob = checkout -b
    p = push
    pa = push --all
    puo = push -u origin
    pfwl = push --force-with-lease
    l = pull
    lrb = pull --rebase
    f = fetch
    fa = fetch --all
    r = remote
    rpo = remote prune origin
    df = diff
    in = init
    cl = clone
    rmc = rm --cached
    sh = stash
    shp = stash pop
    shdr = stash drop
    bis = bisect
    crp = cherry-pick
    lg = log --oneline --decorate --graph
    lga = log --oneline --decorate --graph --all
    lgs = log --graph --pretty='%n%Cred%h%Creset -%C(auto)%d%Creset %s %Cgreen(%ar) %C(bold blue)<%an>%Creset' --stat
    wt = worktree
    res = reset
    resh = reset --hard
    smod = submodule
    smodur = submodule update --init --recursive
